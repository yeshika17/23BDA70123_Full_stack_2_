const Header =({title})=>{
    return(
        <header style ={{padding:'20px', textAlign:'center', backgroundColor:'#28342c', color:'white'}}>
        <h1>
            {title}
        </h1>
        </header>
    )
}
export default Header;